{
  "name": "canam-weather-app",
  "private": true,
  "scripts": {
    "setup": "npm install && npm install --prefix frontend && dotnet restore backend/NwsWeather.Api",
    "dev": "concurrently -k -n backend,frontend -c blue,green \"npm run backend\" \"npm run frontend\"",
    "backend": "dotnet run --project backend/NwsWeather.Api",
    "frontend": "npm run dev --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}